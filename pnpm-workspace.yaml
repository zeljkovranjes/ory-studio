# pnpm 10 reads dependency overrides from here (not package.json).
overrides:
  # Force-patch a transitive postcss XSS advisory (GHSA-qx2v-qp2m-jg93)
  # pulled in by Next.js. Build-time only here, but kept patched for hygiene.
  postcss@<8.5.10: ">=8.5.10"
