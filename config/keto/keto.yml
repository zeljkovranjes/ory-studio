# Keto configuration — owned by the ory-studio config engine.
version: v0.14.0

dsn: memory # overridden by DSN env var in docker-compose

serve:
  read:
    host: 0.0.0.0
    port: 4466
  write:
    host: 0.0.0.0
    port: 4467

namespaces:
  location: file:///etc/config/keto/namespaces.keto.ts

log:
  level: info
  format: json
